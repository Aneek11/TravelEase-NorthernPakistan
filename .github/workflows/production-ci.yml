name: Production CI

on:
  pull_request:
    branches: [production]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run HTML Lint
        run: npx htmlhint "**/*.html" || true
